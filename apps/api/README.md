# Restaurant Service API

Kotlin, Spring Boot, Spring MVC, Spring Security, and Spring Data JPA based API module.

This initial skeleton uses an in-memory H2 datasource so the application context can start before the MySQL/Flyway work lands in the next backend step.

## Commands

```bash
./gradlew :apps:api:build
./gradlew :apps:api:bootRun
```
