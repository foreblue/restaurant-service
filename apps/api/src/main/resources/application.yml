spring:
  application:
    name: restaurant-service-api
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  profiles:
    default: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    validate-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: false

logging:
  structured:
    format:
      console: logstash

app:
  auth:
    session:
      cookie-name: ${BUSINESS_SESSION_COOKIE_NAME:BUSINESS_SESSION}
      ttl-seconds: ${BUSINESS_SESSION_TTL_SECONDS:28800}
      cookie-secure: ${BUSINESS_SESSION_COOKIE_SECURE:true}
    reservation-lookup:
      ttl-seconds: ${RESERVATION_LOOKUP_TOKEN_TTL_SECONDS:86400}
    password-reset:
      ttl-seconds: ${BUSINESS_PASSWORD_RESET_TTL_SECONDS:1800}
      request-interval-seconds: ${BUSINESS_PASSWORD_RESET_REQUEST_INTERVAL_SECONDS:60}
      expose-token-in-response: ${BUSINESS_PASSWORD_RESET_EXPOSE_TOKEN_IN_RESPONSE:false}
    initial-owner:
      enabled: ${INITIAL_OWNER_ENABLED:false}
      email: ${INITIAL_OWNER_EMAIL:owner@example.com}
      password: ${INITIAL_OWNER_PASSWORD:}
      display-name: ${INITIAL_OWNER_DISPLAY_NAME:Initial Owner}
  file-storage:
    local:
      root-path: ${LOCAL_FILE_STORAGE_ROOT_PATH:/tmp/restaurant-service-files}
      public-base-url: ${LOCAL_FILE_STORAGE_PUBLIC_BASE_URL:}
